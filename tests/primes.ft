function is_prime(n) {
  if (n < 2) return false;
  let d = 2;
  while (d * d <= n) {
    if (n % d == 0) return false;
    d = d + 1;
  }
  return true;
}

let n = 2;
while (n <= 30) {
  if (is_prime(n)) println(n);
  n = n + 1;
}
