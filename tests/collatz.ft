function collatz(n) {
  let steps = 0;
  println(`start: ${n}`);

  while (n != 1) {
    if (n % 2 == 0) n = n / 2;
    else n = n * 3 + 1;
    steps = steps + 1;
  }

  return steps;
}

println(`steps to reach 1: ${collatz(27)}`);
