println(`host: ${$`hostname`}`);
println(`user: ${$`whoami`}`);
println(`cwd: ${$`pwd`}`);

const dir = 'tests';
const files = exec(`ls ${dir}`);
println('test files:');
println(files);

const ip = fetch('https://api.ipify.org');
println(`public ip: ${ip}`);

const count = trim($`ls tests/ | wc -l`);
println(`num tests: ${count}`);
